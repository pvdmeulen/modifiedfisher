% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/local_size_randomised.R
\name{local_size_randomised}
\alias{local_size_randomised}
\title{Local size of the randomised (UMPU) Fisher Exact Test The existing
local_size() uses mod_fe_test(), which applies a gamma0 threshold and returns
0 or 1. That works for the modified and conservative tests, but the fully
randomised test genuinely randomises at the boundary: it rejects with
probability gamma1 (lower) or gamma2 (upper), not deterministically. This
function computes that expected size directly. Find the local size of the
MFET as a function of the null OR theta_0.}
\usage{
local_size_randomised(nuisance, .m, .n, .df, .odds_ratio, .alpha, .precision)
}
\arguments{
\item{nuisance}{Nuisance parameter.}

\item{.m}{Integer input responses and sample sizes. Tests u/m versus v/n. No default.}

\item{.n}{Integer input responses and sample sizes. Tests u/m versus v/n. No default.}

\item{.df}{Testing frame (data frame) generated as part as construct_test_frame().}

\item{.odds_ratio}{The null hypothesis odds ratio being tested. No default.}

\item{.alpha}{The nominal significance level α. Defaults to 0.05.}

\item{.precision}{Defines the precision by which confidence limits, p-values, and size is determined. Defaults to 1E-03.}
}
\description{
Local size of the randomised (UMPU) Fisher Exact Test The existing
local_size() uses mod_fe_test(), which applies a gamma0 threshold and returns
0 or 1. That works for the modified and conservative tests, but the fully
randomised test genuinely randomises at the boundary: it rejects with
probability gamma1 (lower) or gamma2 (upper), not deterministically. This
function computes that expected size directly. Find the local size of the
MFET as a function of the null OR theta_0.
}
\keyword{find}
\keyword{hypothesis}
\keyword{local}
\keyword{randomised}
\keyword{randomized}
\keyword{size}
\keyword{test}
