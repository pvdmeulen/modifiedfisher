% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/local_size_mfet.R
\name{local_size_mfet}
\alias{local_size_mfet}
\title{Local size of the MFET}
\usage{
local_size_mfet(
  nuisance,
  .gamma0,
  .odds_ratio,
  .m,
  .n,
  .df,
  .alpha,
  .precision,
  .rejection_matrix = NULL
)
}
\arguments{
\item{nuisance}{The nuisance parameter \eqn{p_0}: success probability in
group 1 under \eqn{H_0}. Must be in \eqn{[0, 1]}.}

\item{.gamma0}{The \eqn{\gamma_0} threshold: boundary values in the test
frame are rejected only when their randomisation probability exceeds this
value. Typically the output of \code{optimise_gamma0()}.}

\item{.odds_ratio}{The null hypothesis odds ratio \eqn{\theta_0}. No default.}

\item{.m}{Number of trials in group 1.}

\item{.n}{Number of trials in group 2.}

\item{.df}{Test frame (data frame) generated by \code{construct_test_frame()},
containing critical values \eqn{c_1}, \eqn{c_2} and randomisation
probabilities \eqn{\gamma_1}, \eqn{\gamma_2} for every possible total \eqn{T}.}

\item{.alpha}{Nominal significance level \eqn{\alpha}. No default.}

\item{.precision}{Numerical precision. No default.}

\item{.rejection_matrix}{Optional prebuilt rejection matrix from
\code{.build_rejection_matrix()}. Defaults to \code{NULL}, in which case
the matrix is built internally from \code{.df} and \code{.gamma0}. Supply
a prebuilt matrix when calling repeatedly with the same test frame and
\eqn{\gamma_0} (as \code{size_mfet()} does across its zoom grid) to avoid
rebuilding it on every call. If supplied, it must correspond to the same
\code{.df} and \code{.gamma0} passed alongside it; this is not checked.}
}
\description{
Computes the unconditional rejection probability (local size) of the MFET
at nuisance parameter \eqn{p_0}, where
\eqn{p_1 = p_0 / (p_0 + \theta_0 (1 - p_0))}. Called repeatedly by
\code{size_mfet()} during numerical optimisation over \eqn{p_0} to find
the maximum size. Computed as the bilinear form \eqn{p_u^\top R \, p_v},
where \eqn{R} is the rejection matrix from
\code{.build_rejection_matrix()} and \eqn{p_u}, \eqn{p_v} are the binomial
probability vectors of the two groups.
}
\seealso{
\code{\link[=local_size_woolf]{local_size_woolf()}}, \code{\link[=local_size_procfreq]{local_size_procfreq()}}, \code{\link[=local_size_randomised]{local_size_randomised()}} for the local size under alternative tests at the same nuisance parameter value; \code{\link[=size_mfet]{size_mfet()}} for the mfet local size maximised over the nuisance parameter; \code{\link[=power_mfet]{power_mfet()}} for the power of the modified Fisher exact test; \code{\link[=modified_fisher_exact_test]{modified_fisher_exact_test()}} for the main user-facing function.

Other size: 
\code{\link{local_size_procfreq}()},
\code{\link{local_size_randomised}()},
\code{\link{local_size_woolf}()},
\code{\link{size_mfet}()}

Other mfet: 
\code{\link{construct_test_frame}()},
\code{\link{modified_fisher_exact_test}()},
\code{\link{optimise_gamma0}()},
\code{\link{power_mfet}()},
\code{\link{size_mfet}()}
}
\concept{mfet}
\concept{size}
\keyword{internal}
