% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/size_mfet.R
\name{size_mfet}
\alias{size_mfet}
\title{Actual size of the MFET for a given \eqn{\gamma_0}}
\usage{
size_mfet(
  .c,
  .odds_ratio,
  .m,
  .n,
  .df,
  .alpha,
  .precision,
  .method,
  .maze,
  .zoom_iter
)
}
\arguments{
\item{.c}{The \eqn{\gamma_0} value for which the actual size is to be
computed. Supplied as a length-1 list element; typically one element of
the sorted randomisation probability vector from the test frame.}

\item{.odds_ratio}{The null hypothesis odds ratio \eqn{\theta_0}. No default.}

\item{.m}{Number of trials in group 1.}

\item{.n}{Number of trials in group 2.}

\item{.df}{Test frame (data frame) generated by \code{construct_test_frame()},
containing critical values \eqn{c_1}, \eqn{c_2} and randomisation
probabilities \eqn{\gamma_1}, \eqn{\gamma_2} for every possible total \eqn{T}.}

\item{.alpha}{Nominal significance level \eqn{\alpha}. No default.}

\item{.precision}{Numerical precision. No default.}

\item{.method}{Numerical method: \code{"zoom"} or \code{"trust"}.}

\item{.maze}{Number of grid points evaluated at each zoom iteration.}

\item{.zoom_iter}{Number of zoom iterations (\code{"zoom"} method only).}
}
\description{
Finds the actual size of the MFET for a given \eqn{\gamma_0} by numerically
maximising the local size over the nuisance parameter \eqn{p_0 \in (0, 1)}.
Supports two methods: \code{"zoom"} evaluates the local size at a grid of
points and iteratively refines the maximum (matches the SAS macro default);
\code{"trust"} uses a trust-region optimiser with the analytic gradient from
\code{.local_size_gradient_mfet()}.
}
\seealso{
\code{\link[=optimise_gamma0]{optimise_gamma0()}} which maximises this quantity to find the optimal gamma0; \code{\link[=local_size_mfet]{local_size_mfet()}} for the local size at a fixed nuisance parameter value, of which this is the maximum; \code{\link[=local_size_woolf]{local_size_woolf()}}, \code{\link[=local_size_procfreq]{local_size_procfreq()}}, \code{\link[=local_size_randomised]{local_size_randomised()}} for the equivalent quantity under alternative tests.

Other mfet: 
\code{\link{construct_test_frame}()},
\code{\link{local_size_mfet}()},
\code{\link{modified_fisher_exact_test}()},
\code{\link{optimise_gamma0}()},
\code{\link{power_mfet}()}

Other size: 
\code{\link{local_size_mfet}()},
\code{\link{local_size_procfreq}()},
\code{\link{local_size_randomised}()},
\code{\link{local_size_woolf}()}
}
\concept{mfet}
\concept{size}
