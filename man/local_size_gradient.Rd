% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/local_size_gradient.R
\name{local_size_gradient}
\alias{local_size_gradient}
\title{Local size gradient of the MFET}
\usage{
local_size_gradient(
  nuisance,
  .gamma0,
  .odds_ratio,
  .m,
  .n,
  .df,
  .alpha,
  .precision
)
}
\arguments{
\item{nuisance}{The nuisance parameter \eqn{p_0}: success probability in
group 1 under \eqn{H_0}. Must be in \eqn{[0, 1]}.}

\item{.gamma0}{The \eqn{\gamma_0} threshold: boundary values in the test
frame are rejected only when their randomisation probability exceeds this
value. Typically the output of \code{optimise_gamma0()}.}

\item{.odds_ratio}{The null hypothesis odds ratio \eqn{\theta_0}. No default.}

\item{.m}{Number of trials in group 1.}

\item{.n}{Number of trials in group 2.}

\item{.df}{Test frame (data frame) generated by \code{construct_test_frame()},
containing critical values \eqn{c_1}, \eqn{c_2} and randomisation
probabilities \eqn{\gamma_1}, \eqn{\gamma_2} for every possible total \eqn{T}.}

\item{.alpha}{Nominal significance level \eqn{\alpha}. No default.}

\item{.precision}{Numerical precision. No default.}
}
\description{
Computes the first derivative of the local size with respect to the nuisance
parameter \eqn{p_0}, using the analytic derivative of the binomial PMF.
Used by \code{mfet_size()} when \code{method = "trust"} to supply the
gradient to the trust-region optimiser.
}
\keyword{find}
\keyword{gradient}
\keyword{hypothesis}
\keyword{local}
\keyword{size}
\keyword{test}
