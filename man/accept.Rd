% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accept.R
\name{accept}
\alias{accept}
\title{Accept/reject helper}
\usage{
accept(
  z,
  .odds_ratio,
  .m,
  .n,
  .df,
  .alpha,
  .precision,
  .method,
  .maze,
  .zoom_iter
)
}
\arguments{
\item{z}{Length-2 integer vector \eqn{(u, t)}, where \eqn{u} is the
observed number of successes in group 1 and \eqn{t = u + v} is the total
number of successes.}

\item{.odds_ratio}{The null hypothesis odds ratio \eqn{\theta_0}. No default.}

\item{.m}{Number of trials in group 1.}

\item{.n}{Number of trials in group 2.}

\item{.df}{Test frame (data frame) generated by \code{construct_test_frame()},
containing critical values \eqn{c_1}, \eqn{c_2} and randomisation
probabilities \eqn{\gamma_1}, \eqn{\gamma_2} for every possible total \eqn{T}.}

\item{.alpha}{Nominal significance level \eqn{\alpha}. No default.}

\item{.precision}{Numerical precision for p-values, confidence limits, and
size calculations. No default.}

\item{.method}{Numerical method for maximising the local size over the
nuisance parameter. Either \code{"zoom"} (default in the main function) or
\code{"trust"} (uses the trust-region method from the trust package).}

\item{.maze}{Number of grid points evaluated at each zoom iteration.}

\item{.zoom_iter}{Number of zoom iterations (\code{"zoom"} method only).}
}
\description{
Determines whether \eqn{H_0}: OR = \code{.odds_ratio} is accepted for the
observed table \eqn{z = (u, t = u + v)}. First finds the optimal
\eqn{\gamma_0} via \code{optimise_gamma0()}, then evaluates the MFET
rejection indicator via \code{mod_fe_test()}. Returns 1 (accept) or
0 (reject). Used in the bisection searches for confidence limits and
p-values in \code{modified_fisher_exact_test()}, and not exported.
}
\keyword{accept}
\keyword{exact}
\keyword{fisher}
\keyword{hypothesis}
\keyword{modified}
\keyword{reject}
\keyword{test}
